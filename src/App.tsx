import { createUseStyles } from 'react-jss'

const useStyles = createUseStyles({
  display: 'flex'
})

function App() {
  const styles = useStyles()

  return (
    <>
      READY PLAYER1
    </>
  )
}

export default App
